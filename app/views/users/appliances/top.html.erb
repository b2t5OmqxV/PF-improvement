<%= link_to '新規投稿', new_appliance_path %>
<h1>カテゴリ</h1>
<% @categories.each do |category| %>
<%= link_to ({controller: 'users/appliances', action: 'index', id: category.id }) do %>
<%= category.category_name %>
<% end %>
<% end %>

<h2>ようこそ、家電 ChangeRecorder</h2>
<p>家電製品の購入日や稼働開始日を登録すると保証期限や有効寿命が経過する前に通知を行い、他の利用者の投稿内容や稼働期間を閲覧できることで買い替え時の参考にできるサイトです。</p>

<h1>お知らせ</h1>
<% @notices.each do |notice| %>
<%= notice.created_at %>
<%= notice.title %>
<%= link_to '詳細',notice_path(notice.id) %>
<% end %>

<h2>最近の投稿</h2>
<% @appliances.first(8).each do |appliance| %>
<%= link_to appliances_path(appliance.id) do %>
<%= attachment_image_tag appliance, :image, :fill, 100, 100, size: '150x150' %>
<% end %>
<%= appliance.category.category_name %>
<%= appliance.maker %>
<%= appliance.product %>
<% end %>


