<table class='my-5 table text-center' style='table-layout:fixed;'>
  <thead>
    <tr>
    <% if admin_signed_in? %>
      <th style='width: 15%'>通知ID</th>
	    <th style='width: 15%'>通知日</th>
	    <th style='width: 25%'>タイトル</th>
	    <th style='width: 25%'>詳細内容</th>
	    <th style='width: 20%' colspan='1'></th>
    <% else %>
      <th>通知日</th>
      <th>タイトル</th>
      <th>詳細内容</th>
    <% end %>
    </tr>
  </thead>
  <tbody>
    <tr>
    <% if admin_signed_in? %>
      <th><%= notice.id %></th>
      <td><%= notice.created_at.strftime('%Y/%m/%d') %></td>
      <td><%= notice.title %></td>
      <td><%= notice.text %></td>
      <td>
        <%= link_to '編集',edit_admins_notice_path(notice.id), class:'mx-1 btn btn-outline-primary' %>
        <%= link_to '削除',admins_notice_path(notice.id), method: :delete, class:'mx-1 btn btn-outline-danger' %>
      </td>
    <% else %>
      <th><%= notices.created_at.strftime('%Y/%m/%d') %></th>
      <td><%= notices.title %></td>
      <td><%= notices.text %></td>
     <% end %>
     </tr>
  </tbody>
</table>
